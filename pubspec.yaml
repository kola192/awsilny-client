name: awsilny_client
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter 

  firebase_core: ^1.19.1
  firebase_auth: ^3.4.1
  cloud_firestore: ^3.2.1
  provider: ^6.0.3
  flutter_spinkit: ^5.1.0
  cupertino_icons: ^1.0.2
  flutter_launcher_icons: "^0.9.2"
  flutter_map: any
  geocoding: ^2.0.4
  tuple: ^2.0.0
  latlong: ^0.6.1
  positioned_tap_detector_2: ^1.0.4
  transparent_image: ^2.0.0
  async: ^2.8.0
  flutter_image: ^4.1.3
  vector_math: ^2.1.2
  proj4dart: ^2.0.0
  meta: ^1.7.0
  collection: ^1.16.0
  location: ^4.4.0

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/icons/app_icon.png"

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  
flutter:
  uses-material-design: true
  assets:
     - images/